html
  head
    include _common_header.pug
  body
    include _navbar.pug
    .container

      if (currentUser && currentUser.id === user.id)
        a.pull-right(href='/edit') Edit your profile

      h1=user.name()
      h2 Joined on #{user.get('createdAt')}

      if user.bio
        p=user.bio
